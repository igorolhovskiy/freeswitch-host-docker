<!--
    NOTICE:

    This context is usually accessed via the external sip profile listening on port 5080.
    
    It is recommended to have separate inbound and outbound contexts.  Not only for security
    but clearing up why you would need to do such a thing.  You don't want outside un-authenticated
    callers hitting your default context which allows dialing calls thru your providers and results 
    in Toll Fraud.
-->

<!-- http://wiki.freeswitch.org/wiki/Dialplan_XML -->
<include>
  <context name="pai-test">

    <extension name="pai-test">
      <condition field="destination_number" expression="^1024$"/>
        <condition field="${sip_P-Asserted-Identity}" expression="^\+?33\d{9}$" break="never">

          <condition field="${sip_from_user}" expression="^[Aa]nonymous$" break="never">
            <action application="log" data="P1-2"/>
          </condition>

          <condition>
            <action application="log" data="P1-1"/>
            <action application="answer"/>
            <action application="sleep" data="1000"/>
            <action application="hangup"/>
          </condition>

        </condition>

        <condition>
          <action application="log" data="P2-1"/>
          <action application="hangup"/>
        </condition>
      
    </extension>
  </context>
</include>
